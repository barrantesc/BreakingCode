{
  "name": "breakingcode",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "client": "cd client && npm start",
    "dev": "concurrently -n 'server, client' -c 'yellow,blue' \"node server/server.js\" \"npm run client\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build",
    "heroku-postbuild": "cd client && npm i && cd ../server && npm run seed && cd ../client && npm run build"

  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/barrantesc/BreakingCode.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.26.1",
    "concurrently": "^7.1.0",
    "express": "^4.17.3",
    "mongoose": "^6.2.11",
    "morgan": "^1.10.0"
  }
}
